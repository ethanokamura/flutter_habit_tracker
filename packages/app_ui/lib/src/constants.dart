import 'package:flutter/material.dart';

// constants
const double defaultPadding = 10;
const double defaultSpacing = 10;
const double defaultRadius = 5;
const BorderRadius defaultBorderRadius =
    BorderRadius.all(Radius.circular(defaultRadius));
const String defaultDarkImage = 'assets/dark_mode_face.png';
const String defaultLightImage = 'assets/light_mode_face.png';
